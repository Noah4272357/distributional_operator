Use solution.hdf5 as input, initial.hdf5 as output, split the data use 400 and 100 data size as train and valid data.

Use deep_set_conditional_flow model, SinkhornLoss, Estimate the time and memory usage when B=8, N_sample=200, Nx=Ny=256, num_samples=200 for 20 epochs. 